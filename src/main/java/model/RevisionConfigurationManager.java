package model;

public interface RevisionConfigurationManager extends ConfigurationManager<RevisionConfiguration>{
    /*
    RevisionConfiguration getRevisionConfiguration();
    void updateRevisionConfiguration(RevisionConfiguration revisionConfiguration);
    */
}

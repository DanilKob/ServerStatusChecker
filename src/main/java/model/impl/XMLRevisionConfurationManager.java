package model.impl;

import model.RevisionConfiguration;
import model.RevisionConfigurationManager;

public class XMLRevisionConfurationManager implements RevisionConfigurationManager {
    @Override
    public RevisionConfiguration getConfiguration() {
        return null;
    }

    @Override
    public void updateConfiguration(RevisionConfiguration configuration) {

    }
}
